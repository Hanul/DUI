/**
 * Menu class
 */
DUI.MENU = CLASS({

	preset : function() {
		'use strict';

		return UUI.BUTTON_H;
	},

	init : function(inner, self, params) {
		'use strict';
		//REQUIRED: params
		//OPTIONAL: params.menus
		
	}
});
